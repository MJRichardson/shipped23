name: Release - Pull Request

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-release:
    uses: MJRichardson/shipped23/.workflows/build-and-release.yaml@release-main
    with:
      octopus-channel: "Branches"
